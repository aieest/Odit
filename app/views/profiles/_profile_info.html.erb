<div class="profile-info-home">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Your Profile</h2>
    <p class="mb-2"><strong>Given Name:</strong> <%= profile.given_name %></p>
    <p class="mb-2"><strong>Last Name:</strong> <%= profile.last_name %></p>
    <p class="mb-2"><strong>Gender:</strong> <%= profile.gender %></p>
    <p class="mb-2"><strong>Balance:</strong> <%= number_to_currency(profile.balance) %></p>
    
    <%= link_to 'Edit Profile', edit_profile_path(profile), class: 'bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded transition duration-300 mt-4 inline-block' %>
</div>