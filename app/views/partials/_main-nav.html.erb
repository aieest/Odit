<% if current_user.profile && current_user.profile.complete? %>
    <%= link_to 'Home', home_path, class: 'main-nav-btn' %>
    <%= link_to 'Earnings', profile_earnings_path(current_user.profile), class: 'main-nav-btn' %>
    <%= link_to 'Expenses', profile_expenses_path(current_user.profile), class: 'main-nav-btn' %>
    <!--
    <%= link_to 'Profile', profile_path(current_user.profile), class: 'main-nav-btn' %>
    <%= link_to 'Edit Profile', edit_profile_path(current_user.profile), class: 'main-nav-btn' %>
    <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'main-nav-btn' %>
    -->
    <i class="main-nav-btn">Graph</i>
    <i class="main-nav-btn">Settings</i>
<% else %>
    <!--Disabled nav links-->
    <i class="main-nav-btn">Home</i>
    <i class="main-nav-btn">Earnings</i>
    <i class="main-nav-btn">Expenses</i>
    <i class="main-nav-btn">Graph</i>
    <i class="main-nav-btn">Settings</i>
<% end %>